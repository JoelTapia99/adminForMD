const config = {
    port: 3000,
    origin: 'http://localhost:4200',
    db: {
        host: 'localhost',
        user: 'postgres',
        password: 'joel',
        database: 'gameOfMD'
    }
}

module.exports = config;