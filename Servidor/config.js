const config = {
    port: 3000,
    origin: 'http://localhost:4200',
    db: {
        host: 'localhost',
        user: 'postgres',
        password: '12345',
        database: 'BDD-pruebas'
    }
}

module.exports = config;