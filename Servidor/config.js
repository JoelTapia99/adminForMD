const config = {
    port: 3000,
    origin: 'http://localhost:4200',
    db: {
        host: 'localhost',
        user: 'postgres',
        password: ['joel', '12345'],
        database: ['gameOfMD', 'BDD-pruebas']
    }
}

module.exports = config;